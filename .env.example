# Absolute path to the repo/directory holding the raw Kaggle corpus
# (expects data/replays/corpus*, data/EN_Card_Data.csv underneath it)
SOURCE_DATA_DIR=/Users/emiloroz/workplace/pokemon-kaggle

# Where this pipeline writes its lake + warehouse (default: ./data)
# PIPELINE_DATA_DIR=./data

# Optional cloud path — leave unset for fully-local runs
# S3_BUCKET=
